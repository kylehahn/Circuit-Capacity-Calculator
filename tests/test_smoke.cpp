// Placeholder smoke test. Replace as the real test suite grows
// (mirror the src/ layout under tests/).

#include <catch2/catch_test_macros.hpp>

TEST_CASE("scaffold smoke", "[scaffold]") {
    REQUIRE(2 + 2 == 4);
}
